name: Windows x64

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc
      - run: cargo build --release --target x86_64-pc-windows-msvc
      - run: mkdir artifact && copy target\x86_64-pc-windows-msvc\release\rustkpcli.exe artifact\
      - uses: actions/upload-artifact@v4
        with:
          name: rustkpcli-windows-x64
          path: artifact\
